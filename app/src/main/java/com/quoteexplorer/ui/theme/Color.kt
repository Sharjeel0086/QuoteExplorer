package com.quoteexplorer.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors - matching the warm yellow design system
val PrimaryYellow = Color(0xFFFFC727)
val AccentGold = Color(0xFFFFB800)

// Neutral Colors
val DarkCharcoal = Color(0xFF1A1A1A)
val SoftGray = Color(0xFF7A7A7A)
val LightGray = Color(0xFFB8B8B8)

// Surface Colors
val SurfaceWhite = Color(0xFFFFFFFF)
val Background = Color(0xFFF6F6F6)

// Semantic Colors
val ErrorRed = Color(0xFFE53935)
val SuccessGreen = Color(0xFF43A047)

// Shadow Color for cards
val CardShadow = Color(0x33000000)

// Favorite heart colors
val FavoriteActive = PrimaryYellow
val FavoriteInactive = Color(0xFFE0E0E0)

// Gradient colors for hero section
val HeroGradientStart = PrimaryYellow
val HeroGradientEnd = AccentGold
